// mock anything you want to mock during jest setup.
// for example, define new HTML elements, mock external dependencies, etc.
// ideally, if you have multiple mocks, I would recommend writing a mockfile
// for each and `require()`ing them here (don't import, just require).
//
